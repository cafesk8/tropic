{% import '@ShopsysShop/Front/Content/Product/productListMacro.html.twig' as productList %}

{% if products|length > 0 %}
    <div class="web__line">
        <div class="web__container">
            <div class="box-last-visited">
                <h2 class="box-last-visited__title">
                    {{ 'Naposledy prohlížené produkty'|trans }}
                </h2>

                {{ productList.horizontalList(products, variantsIndexedByMainVariantId, "list-last-visited", 'h3', 'last-visited') }}
            </div>
        </div>
    </div>
{% endif %}
