{% for article in articles %}
    <li class="top-bar__menu__item">
        <a href="{{ url('front_article_detail', { id: article.id }) }}" class="top-bar__menu__item__link">
            {{ article.name }}
        </a>
    </li>
{% endfor %}
