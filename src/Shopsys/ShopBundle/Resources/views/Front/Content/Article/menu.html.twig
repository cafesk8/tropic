<ul class="menu dont-print">
    {% for article in articles %}
        <li class="menu__item">
            <a class="menu__item__link underline" href="{{ url('front_article_detail', { id: article.id }) }}">{{ article.name }}</a>
        </li>
    {% endfor %}
</ul>
