<div class="list-menu">
    <ul class="list-menu__column dont-print">
        {% for article in articles %}
            <li class="list-menu__item">
                <a class="list-menu__item__link underline" href="{{ url('front_article_detail', { id: article.id }) }}">{{ article.name }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
