{% extends '@ShopsysShop/Front/Layout/layoutWithoutPanel.html.twig' %}

{% block javascripts_bottom %}
    {{ parent() }}
    <script type="text/javascript">
        {% for payment in payments %}
            {% for transport in payment.transports %}
                Shopsys.order.addPaymentTransportRelation({{ payment.id }}, {{ transport.id }});
            {% endfor %}
        {% endfor %}
    </script>
{% endblock %}

{% block meta_robots -%}
    <meta name="robots" content="noindex, follow">
{% endblock %}

{% block title %}
    {{ 'Order [noun]'|trans }}
{% endblock %}

{% block breadcrumb %}
{% endblock %}

{% block infoRow %}
{% endblock %}

{% block header %}
    {% set shopInfoPhoneNumber = getShopInfoPhoneNumber() %}
    {% set shopInfoPhoneHours = getShopInfoPhoneHours() %}

    <div class="web__container">
        <header class="header header--small">
            <div class="header__logo">
                {% block logo %}
                    <a class="logo" href="{{ url('front_homepage') }}">
                        <img src="{{ asset('assets/frontend/images/logo.png') }}" alt="{{ 'Online shop'|trans }}">
                    </a>
                {% endblock %}
            </div>
            <div class="header__steps">
                {{ flow.getCurrentStepNumber() }} / <span class="header__steps__count">3</span> {{ 'Doprava a platba'|trans }}
            </div>
            <div class="header__contact dont-print">
                <div class="header__contact__text">
                    {{ 'Potřebjete poradit s dokončenímobjednávky?'|trans }}<br>
                    {{ shopInfoPhoneHours }}
                </div>
                <div class="header__contact__phone">
                    <i class="svg svg-phone"></i>
                    <a href="tel:{{ shopInfoPhoneNumber }}" class="header__contact__phone__number underline">{{ shopInfoPhoneNumber }}</a>
                </div>
            </div>
        </header>
    </div>
{% endblock %}

{% block main_content %}
    {{ form_start(form, { attr: { id: "js-order-form", 'data-ajax-save-url': url('front_order_save_form') }}) }}
        {{ form_errors(form) }}
        {% include '@ShopsysShop/Front/Content/Order/step' ~ flow.getCurrentStepNumber() ~ '.html.twig' %}
    {{ form_end(form) }}
{% endblock %}

{% block footer__links %}{% endblock %}
